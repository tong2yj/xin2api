# 🚀 手动推送到 GitHub - 超简单版

## 复制粘贴这些命令就行！

### 第一步：打开命令提示符

1. 按 `Win + R`
2. 输入 `cmd`
3. 按回车

---

### 第二步：复制粘贴以下命令（一次一行）

```cmd
cd D:\cc\CatieCli-main
```
按回车，应该看到路径变为 `D:\cc\CatieCli-main>`

---

```cmd
git init
```
按回车，应该看到 `Initialized empty Git repository...`

---

```cmd
git add .
```
按回车（可能没有输出，这是正常的）

---

```cmd
git commit -m "添加 Antigravity 反代功能"
```
按回车，应该看到很多文件被提交

---

### 第三步：创建 GitHub 仓库

1. 打开浏览器
2. 访问：https://github.com/new
3. 填写：
   - Repository name: `CatieCli`
   - 选择 `Private`
   - **不要勾选**任何选项
4. 点击 `Create repository`
5. 复制页面上的仓库地址（类似 `https://github.com/你的用户名/CatieCli.git`）

---

### 第四步：连接仓库并推送

**替换下面的地址为你刚才复制的地址**，然后粘贴到命令提示符：

```cmd
git remote add origin https://github.com/你的用户名/CatieCli.git
```
按回车

---

```cmd
git branch -M main
```
按回车

---

```cmd
git push -u origin main
```
按回车

**这时会弹出登录窗口**：
- 用户名：输入你的 GitHub 用户名
- 密码：**不要输入密码！** 需要使用 Token

---

### 第五步：生成 GitHub Token

1. 访问：https://github.com/settings/tokens
2. 点击 `Generate new token (classic)`
3. Note 填写：`CatieCli`
4. Expiration 选择：`No expiration`
5. 勾选：`repo`（所有子选项）
6. 滚动到底部，点击 `Generate token`
7. **复制 Token**（绿色的一长串字符）
8. 回到命令提示符登录窗口，粘贴 Token 作为密码

---

### 第六步：等待推送完成

应该看到类似输出：
```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
...
To https://github.com/你的用户名/CatieCli.git
 * [new branch]      main -> main
```

✅ **成功！**

---

### 第七步：配置 Zeabur 自动部署

1. 访问：https://zeabur.com
2. 登录（使用 GitHub 账号）
3. 进入你的 CatieCli 项目
4. 点击服务卡片
5. 点击 `Settings`
6. 找到 `Git Repository`
7. 点击 `Connect to GitHub`
8. 选择你的 `CatieCli` 仓库
9. 选择 `main` 分支
10. 保存

---

### 第八步：等待部署

- Zeabur 会自动开始部署
- 状态：Building → Deploying → Running
- 大约 2-3 分钟完成

---

## 🧪 测试

部署完成后，测试新功能：

```bash
curl https://你的域名.zeabur.app/antigravity/v1/chat/completions \
  -H "Authorization: Bearer cat-你的api-key" \
  -H "Content-Type: application/json" \
  -d "{\"model\": \"gemini-2.5-flash\", \"messages\": [{\"role\": \"user\", \"content\": \"Hello\"}]}"
```

---

## ❓ 遇到问题？

### 问题1：git 不是内部或外部命令

**解决**：需要安装 Git
- 下载：https://git-scm.com/download/win
- 安装后重启命令提示符

### 问题2：推送时提示 Permission denied

**解决**：Token 权限不足
- 重新生成 Token
- 确保勾选了 `repo` 权限

### 问题3：推送时提示 Repository not found

**解决**：仓库地址错误
- 检查地址是否正确
- 确保仓库已创建

---

## 📞 需要帮助

告诉我：
1. 在哪一步卡住了
2. 看到什么错误信息
3. 截图发给我
