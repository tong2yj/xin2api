# 数据库配置
# SQLite (默认，适合小型部署):
DATABASE_URL=sqlite+aiosqlite:///./data/gemini_proxy.db
# PostgreSQL (高并发推荐):
# DATABASE_URL=postgresql+asyncpg://用户名:密码@127.0.0.1:5432/数据库名

# JWT 密钥 (请修改为随机字符串)
SECRET_KEY=your-super-secret-key-change-this-in-production

# 管理员账号
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# 服务配置
HOST=0.0.0.0
PORT=10601

# Gemini CLI 配置
GEMINI_API_BASE=https://generativelanguage.googleapis.com

# 用户默认配额 (每日请求数)
DEFAULT_DAILY_QUOTA=100

# 凭证奖励配额 (上传凭证到公共池的奖励)
CREDENTIAL_REWARD_QUOTA=1000

# 注册开关
ALLOW_REGISTRATION=true

# ================================================================
# gcli2api 桥接配置（必需）
# ================================================================
# 重要：CatieCli 已移除独立 OAuth 模式和自带的凭证池功能
# 必须启用 gcli2api 桥接才能使用 Gemini 模型和 Antigravity 模型
# ================================================================

# gcli2api 服务地址
GCLI2API_BASE_URL=http://localhost:7861

# gcli2api API 密码 (用于调用聊天接口)
# 需要与 gcli2api 项目的 API_PASSWORD 保持一致
GCLI2API_API_PASSWORD=catie_gcli2api_secure_password_2026

# gcli2api 面板密码 (用于调用管理接口，如 OAuth)
# 需要与 gcli2api 项目的 PANEL_PASSWORD 保持一致
GCLI2API_PANEL_PASSWORD=catie_gcli2api_panel_password_2026

# ================================================================
# 三端点轮询配置
# ================================================================
# 端点优先级顺序（逗号分隔）
# 可选值: gcli2api, antigravity, openai
# 系统会按顺序尝试每个端点，直到成功或全部失败
ENDPOINT_PRIORITY=gcli2api,antigravity,openai
